

@model System.Collections.Generic.IList<string>      

<h2>index.cshtml ok</h2>

<h3>Email : @ViewData["email"]</h3>

<h3>Tel   : @ViewBag.tel</h3>

<ul class="list-group">
    @foreach (var item in Model)
    {
        <li class="list-group-item">@item</li>
    }

</ul>